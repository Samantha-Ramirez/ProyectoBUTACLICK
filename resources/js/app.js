import './bootstrap';
import { createApp } from 'vue';
import App from './App.vue';

// Aquí importarás tus servicios y store (Pinia/Vuex) según tu diagrama
const app = createApp(App);
app.mount('#app');
