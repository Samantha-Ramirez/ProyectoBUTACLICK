name: CI

on:
    pull_request:
        branches: ['main']
    push:
        branches: ['main']

jobs:
    lint-test-build:
        name: lint-test-build
        runs-on: ubuntu-latest

        services:
            postgres:
                image: postgres:15
                env:
                    POSTGRES_DB: butaclick_ci
                    POSTGRES_USER: admin
                    POSTGRES_PASSWORD: password
                ports:
                    - 5432:5432
                options: >-
                    --health-cmd="pg_isready -U admin -d butaclick_ci"
                    --health-interval=10s
                    --health-timeout=5s
                    --health-retries=5

        env:
            APP_ENV: testing
            APP_KEY: base64:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
            APP_DEBUG: true
            APP_URL: http://localhost
            DB_CONNECTION: pgsql
            DB_HOST: 127.0.0.1
            DB_PORT: 5432
            DB_DATABASE: butaclick_ci
            DB_USERNAME: admin
            DB_PASSWORD: password

        steps:
            - name: Checking out code
              uses: actions/checkout@v4

            - name: Setting up PHP
              uses: shivammathur/setup-php@v2
              with:
                  php-version: '8.3'
                  extensions: mbstring, pdo_pgsql, bcmath, exif, pcntl, gd, zip
                  coverage: none

            - name: Setting up Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '20'
                  cache: npm

            - name: Caching Composer dependencies
              uses: actions/cache@v4
              with:
                  path: ~/.composer/cache
                  key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
                  restore-keys: |
                      ${{ runner.os }}-composer-

            - name: Installing PHP dependencies
              run: composer install --no-interaction --prefer-dist --no-progress

            - name: Installing JS dependencies
              run: npm ci

            - name: Preparing Laravel environment
              run: |
                  cp .env.example .env
                  php artisan key:generate

            - name: Running database migrations
              run: php artisan migrate --force

            - name: Checking backend formatting
              run: composer format:check

            - name: Running backend tests
              run: php artisan test

            - name: Building frontend assets
              run: npm run build
